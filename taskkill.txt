# 查找占用端口5681的进程
netstat -ano | findstr :5681

# 停止占用端口的进程（将<PID>替换为实际的进程ID）
taskkill /PID <PID> /F

weather:
  api: "http://t.weather.itboy.net/api/weather/city/"

  npm run dev  在当前 PowerShell 中临时绕过
  Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass